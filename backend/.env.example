# Hostinger supplies PORT automatically. Keep 4000 for local development.
PORT=4000

# Store the real value only in Hostinger environment variables or secrets.
# Never commit the real key.
GEMINI_API_KEY=""
